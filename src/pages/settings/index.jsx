import React from 'react';
import Settings from '../../components/settings/Settings';
import Layout from '../../components/layout/Layout';

const SettingsPage = () => {
    return ( <Layout children={<Settings />} /> )
}

export default SettingsPage